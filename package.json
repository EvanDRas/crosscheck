{
  "name": "crosscheck",
  "version": "1.0.0",
  "description": "Crosscheck — the honest stock analyzer. SEC-verified fundamentals, transparent scoring, a private track record that grades you, and published backtests of its own formula.",
  "license": "MIT",
  "type": "module",
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "batch": "node scripts/batch_log.js",
    "test": "node --test test/scoring.test.js test/ledger.test.js test/edgar.test.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "fast-xml-parser": "^5.10.1"
  }
}
